#include "TLAB_CLASS\TLAB_CLASS.cpp"
LRESULT CALLBACK WindowProcedure( HWND hwnd, UINT message, WPARAM wParam, LPARAM lParam );
int WINAPI WinMain (HINSTANCE hThisInstance, HINSTANCE hPrevInstance, LPSTR lpszArgument, int nCmdShow ){
    /// Dlg_ADQ.Show();
    const char szClassName[] = " TLAB ";
    WNDCLASSEX wincl = {0};
    wincl.hInstance = hThisInstance;
    wincl.lpszClassName = szClassName;
    wincl.lpfnWndProc = WindowProcedure;
    wincl.style = CS_DBLCLKS;
    wincl.cbSize = sizeof (WNDCLASSEX);
    wincl.hIcon = LoadIcon( hThisInstance, MAKEINTRESOURCEA(TLAB_ICON) );
    wincl.hIconSm = LoadIcon( hThisInstance, MAKEINTRESOURCEA(TLAB_ICON) );
    wincl.hCursor = LoadCursor( NULL, IDC_ARROW );
    wincl.lpszMenuName = MAKEINTRESOURCEA(DSP_WMenuMain);
    wincl.cbClsExtra = 0;
    wincl.cbWndExtra = 0;
    wincl.hbrBackground = CreateSolidBrush( RGB(0, 0, 0) );/// (HBRUSH) COLOR_BACKGROUND;/// COLOR_WINDOW
    if( !RegisterClassEx (&wincl) ){
        return 0;
    }
    TLC.Create( szClassName );
    return 0;
}
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
LRESULT CALLBACK WindowProcedure( HWND hwnd, UINT message, WPARAM wParam, LPARAM lParam ){
    return (LRESULT)TLC.WProcedure( hwnd, message, wParam, lParam );
}
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
/// /////////////////////////////////////////////////////////////////////////////////////////////////////////////
